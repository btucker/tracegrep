# Benchmark Report: eslint-no-duplicate-imports-unmergeable

## Task Metadata
- Repo: eslint/eslint
- Issue: [#12758](https://github.com/eslint/eslint/issues/12758) [no-duplicate-imports] Treat namespace imports as different
- Human PR: [#14238](https://github.com/eslint/eslint/pull/14238)
- Evaluated agent: `codex`
- Judge agent: `claude`
- Eval ID: `20260310T175219Z`
- Report path: `/Users/btucker/projects/tracegrep/eval/reports/eslint-no-duplicate-imports-unmergeable/codex/20260310T175219Z.md`

## Blind Verdict Summary
- Better matches the accepted PR: `tg`
- Better overall (`control` vs `tg`): `tg`
- Best of all three: `accepted_pr`
- Overall ranking: `accepted_pr` > `tg` > `control`
- Judge confidence: `high`

## Blind Mapping
| Label | Condition |
| --- | --- |
| A | control |
| B | tg |

## Score Table
| Condition | PR Alignment | Reuse Alignment | Duplication Risk | Test Alignment |
| --- | ---: | ---: | ---: | ---: |
| control | 2 | 3 | 2 | 3 |
| tg | 4 | 4 | 4 | 4 |

## Control vs Human PR
### Key differences
- PR stores actual AST nodes and checks mergeability pairwise; A tracks computed state shapes (hasDefault/hasNamed/hasNamespace) instead
- PR unifies imports and exports into a single Map and single handleImportsExports function; A keeps separate importsInFile/exportsInFile objects and separate handlers
- PR uses isImportExportCanBeMerged as a simple boolean check between two nodes; A uses mergeImportStates/absorbExportIntoImportState returning merged state or null
- PR adds 25 lines of docs with explicit 'correct' code blocks for both default and includeExports; A adds only 6 lines of brief prose
- PR adds 59 test lines including multi-import chains and edge cases; A adds 41 lines with fewer edge cases
- PR removes the old 'import os; export * from os' invalid test case; A modifies it to use 'export * as everything' instead of removing it
- PR bumps ecmaVersion to 12; A does not change ecmaVersion

### Strengths
- Thorough JSDoc comments on all new helper functions
- Handles edge case of duplicate default imports explicitly
- Tests export namespace specifier with ecmaVersion 2020 parser option

### Risks
- State-shape tracking is a fundamentally different architecture from the PR; harder to verify correctness for all edge cases
- Complex absorb/merge logic could miss edge cases or produce incorrect merged states
- Does not remove old invalid test case that the PR correctly removes, suggesting possible behavioral difference
- 207 added lines in rule file is high complexity for this change

## TG vs Human PR
### Key differences
- PR unifies into a single modules Map and handleImportsExports; B keeps separate importsInFile/exportsInFile Maps and separate handleImports/handleExports
- PR classifies nodes via getImportExportType returning type strings; B uses getImportSpecifierCounts returning numeric counts and separate isImportMergeable/isExportMergeable/isImportExportMergeable functions
- PR adds 25 lines of docs with full example code blocks; B adds only 3 lines of inline prose
- PR adds 59 test lines including multi-import chains (3-import sequences); B adds 41 lines without those chain tests
- B passes isDuplicate callback to checkAndReport which is cleaner than PR's approach but structurally different
- PR bumps ecmaVersion to 12; B does not change ecmaVersion
- B correctly removes the old 'import os; export * from os' invalid test case, matching the PR

### Strengths
- Stores actual AST nodes like the PR, enabling more accurate mergeability checks
- Clean isDuplicate callback pattern minimizes changes to existing checkAndReport
- Test cases overlap more with the accepted PR (export * duplicates, namespace+named combos)
- Removes the correct old invalid test case matching PR behavior
- Uses Map for storage matching the PR's data structure choice

### Risks
- Keeps separate importsInFile/exportsInFile rather than unifying, meaning the handlers diverge from the PR
- Only 3 lines of doc changes vs 25 in the PR — documentation is insufficient
- Does not bump ecmaVersion in tests like the PR does
- Missing multi-import chain test cases that the PR includes for regression coverage

## Control vs TG
- A tracks computed state shapes ({hasDefault, hasNamed, hasNamespace}); B stores actual AST nodes and checks mergeability pairwise — B is closer to the PR's approach
- A uses Object.create(null) for storage; B uses Map — B is closer to the PR's Map usage
- A has more complex merge logic (mergeImportStates, absorbExportIntoImportState with state mutation); B has simpler predicate functions (isImportMergeable, isExportMergeable, isImportExportMergeable)
- A adds a valid test for duplicate default imports; B adds a valid test for namespace+bare-import edge case
- B removes the old 'import os; export * from os' invalid case matching the PR; A changes it instead
- A's state-tracking approach makes the merge logic harder to verify for correctness; B's pairwise node comparison is more straightforward
- B adds test for 'export * from os; export * from os' as invalid — matching a PR test case A lacks

## Published GitHub Links
Publishing has not been run yet for this evaluation.

Public branch publishing can contaminate future benchmarks, so publish only after the evaluation is complete.

## Final Summary
Implementation B better matches the accepted PR in both architecture and behavior. B stores actual AST nodes and uses pairwise mergeability predicates, closely mirroring the PR's isImportExportCanBeMerged approach. B also correctly removes the old invalid test case for 'import os; export * from os' and adds overlapping test coverage (export * duplicates, namespace+named non-duplicates). Implementation A takes a fundamentally different approach by tracking computed state shapes and using merge/absorb functions that mutate state — this is more complex, harder to verify, and architecturally distant from the PR. Both implementations fall short of the PR in documentation (neither adds proper example code blocks) and test coverage (both have fewer test cases), but B is notably closer overall.
