interface:
  display_name: "Grep with tracegrep"
  short_description: "Search Rust code with call-graph context"
  default_prompt: "Use $tracegrep to search this Rust repo with tracegrep call-graph context."

policy:
  allow_implicit_invocation: true
